<UserControl x:Class="ScreenManagementExample.FileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScreenManagementExample"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Blue" Foreground="Wheat">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Path=PAR1}"/>
            <TextBox x:Name="PAR1"/>
            <TextBlock Text="{Binding Path=PAR2}"/>
            <TextBox x:Name="PAR2"/>
            <Button x:Name="Close" Width="150" Content="Close"/>
        </StackPanel>
            
    </Grid>
</UserControl>
